# =============================================================================
# ITSA WEB PLATFORM — ENVIRONMENT CONFIGURATION TEMPLATE
# =============================================================================
# Copy this file to .env or .env.local and configure as needed for your environment.
# WARNING: NEVER commit .env or .env.local files containing real secrets to Git.

# 1. API Base URL (Optional: defaults to '' which uses Vite proxy /api)
# For local dev with Vite dev server proxy: leave empty or omit
# For direct API server connection: http://localhost:5000/api
VITE_API_BASE_URL=
